import os\n\ndef generate_icon_list():\n    icons_folder = 'icons'  # Specify the folder containing icons\n    icon_list_file = 'icon_list.txt'  # Output file\n    icon_files = []\n\n    # Scan the icons folder for files\n    for root, _, files in os.walk(icons_folder):\n        for file in files:\n            if file.endswith(('.png', '.jpg', '.jpeg', '.gif')):  # Include relevant icon file types\n                icon_files.append(file)\n\n    # Write the list of icon files to the output file\n    with open(icon_list_file, 'w') as f:\n        for icon in icon_files:\n            f.write(icon + '\n')\n\nif __name__ == '__main__':\n    generate_icon_list()\n